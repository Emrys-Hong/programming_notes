package mySecondTut;

public interface Drivable {
	
	// cannot change variables in the interface
	
	int getWheels();
	
	void setWheels(int numWheels);
	
	double getSpeed();
	
	void setSpeed(double speed);
}
